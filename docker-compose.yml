services:
  maintenance:
    image: nginx:alpine
    container_name: maintenance-page
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
    ports:
      - "3000:80"
    restart: unless-stopped